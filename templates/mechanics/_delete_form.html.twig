<form method="post" action="{{ path('mechanics_delete', {'id': mechanic.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mechanic.id) }}">
    <button class="btn">Delete</button>
</form>
